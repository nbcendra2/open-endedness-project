agent:
  params:
    memory_type: enriched_history

env:
  invalid_action_mode: fallback

eval:
  seed: 0
  num_episodes: 5
  max_steps_per_episode: 80
  out_json: runs_tw_gemini_eh/rollout.jsonl

llm:
  name: gemini-2.5-flash-lite
  provider: gemini
  temperature: 0
  timeout: 180.0